import 'package:dashboard/features/show_all_teams/presentation/views/widgets/show_all_teams_body.dart';

import 'package:flutter/material.dart';

class ShowAllTeams extends StatelessWidget {
  const ShowAllTeams({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ShowAllTeamsBody(),
    );
  }
}
